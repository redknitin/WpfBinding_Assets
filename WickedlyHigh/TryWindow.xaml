<Window x:Class="WickedlyHigh.TryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WickedlyHigh"
        Title="TryWindow" Height="350" Width="525" DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid>
        <DataGrid ItemsSource="{Binding Assets}" HorizontalAlignment="Left" Margin="10,61,0,0" VerticalAlignment="Top" Height="248" Width="497" CanUserAddRows="False" 
                  AutoGenerateColumns="True">
            <!--<DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding ID}" Header="ID" />
            </DataGrid.Columns>-->
        </DataGrid>
    </Grid>
</Window>
